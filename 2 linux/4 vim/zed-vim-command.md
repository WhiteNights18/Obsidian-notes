`g d` go to definition
`g shift-d` go to declaration
`g y` go to type definition
`g shift-i` go to implementation
`g shift-a` find all references
`g r n` rename symbol
`g .` show code actions

---
`ctrl-shift-i` format buffer
`shift-delete` cut
`ctrl-insert` copy
`shift-insert` paste
`ctrk-k r` reveal in file manager
``ctrl-` `` terminal